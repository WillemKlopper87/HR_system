name: hcm-ci

on:
  push:
    paths:
      - "hcm/**"
      - ".github/workflows/hcm-ci.yml"
  pull_request:
    paths:
      - "hcm/**"
      - ".github/workflows/hcm-ci.yml"

jobs:
  backend:
    name: Backend (Django checks, migrations, tests — SQLite)
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: hcm/backend
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.13"
          cache: "pip"
          cache-dependency-path: hcm/backend/requirements.lock

      - name: Install dependencies (pinned lock)
        run: pip install -r requirements.lock

      - name: Django system checks
        run: python manage.py check --fail-level WARNING

      - name: Missing-migration guard
        run: python manage.py makemigrations --check --dry-run

      - name: Migrate (SQLite, CI-local)
        run: python manage.py migrate

      - name: Celery app loads and beat schedule resolves
        run: |
          python -c "import django; django.setup(); from config.celery import app; app.loader.import_default_modules(); assert 'rbac_audit.tasks.run_retention_task' in app.tasks; print('celery ok:', sorted(t for t in app.tasks if t.startswith(('rbac_audit','performance','recruitment'))))"
          python manage.py run_retention --dry-run

      - name: Test suite
        run: python manage.py test --parallel auto

  backend-postgres:
    name: Backend (tests — PostgreSQL 17, the production engine)
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: hcm/backend
    services:
      postgres:
        image: postgres:17
        env:
          POSTGRES_DB: hcm
          POSTGRES_USER: hcm
          POSTGRES_PASSWORD: hcm-ci
        ports:
          - 5432:5432
        options: >-
          --health-cmd "pg_isready -U hcm"
          --health-interval 5s
          --health-timeout 3s
          --health-retries 10
    env:
      POSTGRES_HOST: 127.0.0.1
      POSTGRES_PORT: "5432"
      POSTGRES_DB: hcm
      POSTGRES_USER: hcm
      POSTGRES_PASSWORD: hcm-ci
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.13"
          cache: "pip"
          cache-dependency-path: hcm/backend/requirements.lock

      - name: Install dependencies (pinned lock)
        run: pip install -r requirements.lock

      - name: Migrate (PostgreSQL)
        run: python manage.py migrate

      - name: Seed demo data (exercises every app's models on Postgres)
        run: python manage.py seed_demo_data

      - name: Test suite (PostgreSQL)
        run: python manage.py test --parallel auto

  frontend:
    name: Frontend (typecheck, lint, build)
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: hcm/frontend
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: "lts/*"
          cache: "npm"
          cache-dependency-path: hcm/frontend/package-lock.json

      - name: Install dependencies
        run: npm ci

      - name: Lint
        run: npm run lint

      - name: Typecheck + build
        run: npm run build
